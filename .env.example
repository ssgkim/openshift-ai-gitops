# 이 파일을 .env로 복사 후 값을 채우세요. .env 자체는 gitignored.
#   cp .env.example .env

# 기존 OpenShift 클러스터 접속
KUBECONFIG=/absolute/path/to/kubeconfig
CLUSTER_DOMAIN=apps.example.cluster.com
CLUSTER_API_URL=https://api.example.cluster.com:6443

# pull-secret (필요 시)
PULL_SECRET_PATH=/absolute/path/to/pull-secret.json

# Git 원격 (ArgoCD가 이 리포를 source로 사용)
GIT_REMOTE_URL=git@github.com:org/openshift-ai-gitops.git
GIT_BRANCH=main

# 환경 식별자 (DEV|PROD)
OCP_AI_ENV=DEV
