# Source: oc get packagemanifest leader-worker-set -n openshift-marketplace, 2026-04-29.
# stable-v1.0 / currentCSV: leader-worker-set.v1.0.0
# Purpose: satisfy RHOAI KServe LLMInferenceService Wide Expert Parallelism dependency.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: leader-worker-set
  namespace: openshift-lws-operator
spec:
  channel: stable-v1.0
  installPlanApproval: Automatic
  name: leader-worker-set
  source: redhat-operators
  sourceNamespace: openshift-marketplace
