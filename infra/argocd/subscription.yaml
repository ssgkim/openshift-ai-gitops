# 출처: oc get packagemanifest openshift-gitops-operator 2026-04-19 실행 결과
# defaultChannel: latest / currentCSV: openshift-gitops-operator.v1.20.1
# claude-context/version-matrix.md 참조
# 설치 네임스페이스: openshift-operators (global-operators OperatorGroup 기존 존재)
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: openshift-gitops-operator
  namespace: openshift-operators
spec:
  channel: latest
  installPlanApproval: Automatic
  name: openshift-gitops-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
